# Pretrained model weights download automatically on first run.
# yolov8n.pt        — 6.2 MB — 80-class object detection (COCO)
# yolov8n-pose.pt   — 6.5 MB — 17-keypoint multi-person pose
ultralytics>=8.4.0
opencv-python-headless>=4.10
numpy>=1.26
lap>=0.5.12        # tracker dependency for ByteTrack
torch>=2.4         # YOLO backbone (CPU works; GPU optional via torch+cu121)
